\documentclass{article}

%Packages
\usepackage{amsmath} %package for formatting and displaying math
\usepackage{amssymb} %package of math and other symbols
\usepackage{fancyhdr} %package for making headers and footers
\usepackage[normalem]{ulem} %package provides an \ul (underline) command
\usepackage[usenames,dvipsnames]{color} %package provides named colors (e.g. \textcolor{red}{some text}
\usepackage{physics} %package for formatting physics equations
\usepackage{multicol} %package for rendering multiple columns
\usepackage{float} %Improves the interface for defining floating objects such as figures and tables
\usepackage{geometry} %This package provides a flexible and easy interface to page dimensions.
\usepackage{graphicx} %The package builds upon the graphics package, providing a key-value interface for optional arguments to the \includegraphics command.
\usepackage{tabularx} %Version of tabular where col widths are calculated to hit a specified table width
\usepackage{lmodern} %Latin modern fonts
\usepackage{makecell} %formatting cells in tables

%Page setup
\geometry{margin=1in}
\setlength{\parindent}{0pt} %Set the size of indent for first line of a paragraph

\begin{document}

\end{document}
